В папке project представлен проект с кодом, написанный при помощи CMSIS. Для проверки библиотек HAL и LL требуется создать проекты в CubeMX, соответственно подключив нужную библиотеку, а далее изменить функциию main на такую же, что и в проекте с CMSIS, но вызвать функцию соответствующей библиотеки. В самой работе описаны функции, используемые этими библиотеками.
